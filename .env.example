# Local Environment Variables Template
#
# Copy to .env.local:
#   cp .env.example .env.local
#
# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# - Never expose SUPABASE_SERVICE_ROLE_KEY or STRIPE_SECRET_KEY to the browser.

# --- Supabase ---
# Supabase project URL
# Where to get it: Supabase Dashboard → Settings → API → Project URL
# Format: https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url

# Supabase anon/public key
# Where to get it: Supabase Dashboard → Settings → API → Project API keys → anon public
# Format: JWT string (often starts with "eyJ")
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key (server-only)
# Where to get it: Supabase Dashboard → Settings → API → Project API keys → service_role
# Format: JWT string (often starts with "eyJ")
# Recommended for server-side operations like Stripe webhooks + admin scripts
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Optional (recommended) for automated migrations in scripts
# Where to get it: Supabase Dashboard → Settings → Database → Connection string (URI)
# Format: postgres://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres
SUPABASE_DB_URL=postgres://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres

# --- Alchemy ---
# Where to get it: Alchemy Dashboard → Apps → (your app) → API Key
ALCHEMY_API_KEY=your-alchemy-api-key

# --- Stripe ---
# Stripe publishable key (browser-safe)
# Where to get it: Stripe Dashboard → Developers → API keys
# Format: pk_test_... (test) or pk_live_... (live)
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=your-stripe-public-key

# Stripe secret key (server-only)
# Where to get it: Stripe Dashboard → Developers → API keys
# Format: sk_test_... (test) or sk_live_... (live)
STRIPE_SECRET_KEY=your-stripe-secret-key

# Stripe webhook signing secret (server-only)
# Where to get it:
# - Local dev: `stripe listen --forward-to localhost:3000/api/stripe/webhook`
# - Prod: Stripe Dashboard → Developers → Webhooks → (your endpoint) → Signing secret
# Format: whsec_...
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# Production Environment Variables
#
# Copy this file to `.env.production.local` for local production builds,
# or set these values in Vercel → Project → Settings → Environment Variables.
#
# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# - Never expose SUPABASE_SERVICE_ROLE_KEY or STRIPE_SECRET_KEY to the browser.

# --- Supabase ---
# Where to get it: Supabase Dashboard → Settings → API
# Format: https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Where to get it: Supabase Dashboard → Settings → API → Project API keys → anon public
# Format: JWT string (starts with "eyJ...")
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key

# Where to get it: Supabase Dashboard → Settings → API → Project API keys → service_role
# Format: JWT string (starts with "eyJ...")
# Required for server-side operations that must bypass RLS (e.g. Stripe webhooks)
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key

# Optional (recommended) for automated migrations in scripts
# Where to get it: Supabase Dashboard → Settings → Database → Connection string (URI)
# Format: postgres://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres
SUPABASE_DB_URL=postgres://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres

# --- Alchemy ---
# Where to get it: Alchemy Dashboard → Apps → (your app) → API Key
ALCHEMY_API_KEY=your-production-alchemy-api-key

# --- Stripe ---
# Where to get it: Stripe Dashboard (LIVE mode) → Developers → API keys
# Format: pk_live_...
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_live_your_publishable_key

# Where to get it: Stripe Dashboard (LIVE mode) → Developers → API keys
# Format: sk_live_...
STRIPE_SECRET_KEY=sk_live_your_secret_key

# Where to get it:
# - Automated: run `npm run setup:stripe-webhook -- --mode=live --webhook-url=https://<your-domain>/api/stripe/webhook`
# - Manual: Stripe Dashboard (LIVE mode) → Developers → Webhooks → (your endpoint) → Signing secret
# Format: whsec_...
STRIPE_WEBHOOK_SECRET=whsec_your_live_webhook_secret
